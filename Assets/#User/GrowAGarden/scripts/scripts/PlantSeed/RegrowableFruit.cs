using UnityEngine;

namespace GrowAGarden
{
    /// <summary>
    /// I don't know if we need this yet, but it's here if we do
    /// All logic relating to fruits from Regrowable Plants
    /// </summary>
    public class RegrowableFruit : MonoBehaviour
    {

        //TODO: i don't know if we need this yet xD but it's here if we do

        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
